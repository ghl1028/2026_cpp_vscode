# C++23 Labs 프로젝트 - AI 에이전트 자동화 가이드

---

## ⚠️ 핵심 규칙: 학습 단계에서는 코드 변경 금지

**원칙:** 사용자의 **명시적 요청이 없는 이상**, 기존 코드를 수정하거나 구현하지 말 것.

### ✅ 허용되는 활동
- 코드 분석 & 피드백 제공
- 원리 설명 & 개념 가이드
- 힌트와 부분 템플릿 제시
- 링크 제공 (API 문서 등)

### ❌ 금지된 활동 (명시적 요청 없을 시)
- 자동 코드 수정/리팩터링
- "이게 더 좋을 것 같아요" 식의 개선
- 자동 완성 구현

### ✅ 명시적 요청 시 허용 (다음의 경우)
- "이 함수 구현해줄 수 있어?"
- "코드 정리 좀 해줘"
- "이거 어떻게 수정해?"
- **빌드 오류로 실행 불가능한 경우** (CMakeLists.txt 생성 등)
- **새 폴더 생성** (구조 템플릿)

자세한 내용: [AGENTS.md](AGENTS.md) — "🎓 AI 에이전트 학습 코칭 철학" 참조

---

## 🎯 q폴더 신규 생성 패턴

사용자가 "qXX 폴더를 만들어줘" 또는 "새 문제 폴더 생성"을 요청할 때:

### 1️⃣ 항상 포함해야 할 파일 & 디렉토리 구조

```
labs/q{NN}/
├── CMakeLists.txt       ← 🔴 MUST INCLUDE (표준 템플릿)
├── main.cpp             ← 문제별 진입점 또는 소스
└── [선택] *.h / *.cpp   ← 필요한 헤더/구현 파일
```

### 2️⃣ CMakeLists.txt 표준 템플릿 (복사-붙여넣기)

```cmake
cmake_path(GET CMAKE_CURRENT_SOURCE_DIR FILENAME DIR_NAME)
file(GLOB SOURCES CONFIGURE_DEPENDS "*.cpp")
add_executable(${DIR_NAME} ${SOURCES})
```

**중요:** 모든 q폴더는 이 정확한 패턴을 사용합니다.

### 3️⃣ 폴더 생성 순서

1. **디렉토리 생성**: `labs/q{NN}/` 폴더 생성
2. **CMakeLists.txt 생성**: 위 템플릿을 파일에 복사
3. **소스 파일 생성**: 문제에 맞는 `.cpp` / `.h` 생성
4. **CMake 재구성**: 사용자에게 다음 명령 실행 제안
   ```bash
   cmake --preset=default
   cmake --build build --target q{NN}
   ```

### 4️⃣ 코드 스타일 & C++23 준수

- **클래스/타입**: `PascalCase` (예: `Temperature`, `Cell`)
- **함수/변수**: `snake_case` (예: `get_value()`, `item_count`)
- **필수 헤더**: `#include <print>` for output (C++23)
- **범위 알고리즘**: `std::ranges::sort()` 선호 (일반 for 루프 최소화)
- **Enum**: 반드시 `enum class` 사용
- **멤버 변수**: `_` suffix 필수 (예: `value_`, `data_`)

### 5️⃣ 문제 난이도별 권장 구조

| 카테고리 | 예시 폴더 | 파일 구조 | 특징 |
|----------|----------|----------|------|
| **초급** (q01-q03) | q01 | main.cpp 단일 | 스크립트 형, 직렬 처리 |
| **중급** (q25-q28) | q25 | main.cpp | 컨테이너 조작, std::ranges |
| **고급** (q30-q45) | q30 | main.cpp + header | 클래스 설계, .h/.cpp 분리 |
| **심화** (q51-q52) | q51 | main.cpp + Header.h/.cpp | 다중 클래스, 테스트 로직 |

---

## 📝 자동 생성 시 체크리스트

에이전트가 새 q폴더를 생성할 때 **반드시** 확인할 항목:

- ✅ **CMakeLists.txt 포함됨** (표준 템플릿 정확 사용)
- ✅ **최소 1개 이상의 `.cpp` 파일** 포함
- ✅ **C++23 헤더** 포함 (`#include <print>` or `#include <ranges>`)
- ✅ **문제에 맞는 진입점** (main() 또는 문제별 로직)
- ✅ **빌드 완료 후 설명 제시**: "CMake 재구성 후 `cmake --build build --target qXX` 실행해주세요"

---

## 🔧 빌드 & 테스트 명령어

```bash
# CMake 재구성 (새 폴더 추가 후 필수)
cmake --preset=default

# 특정 q폴더 빌드
cmake --build build --target q01

# 실행 (예: q01)
./build/labs/q01/q01.exe

# 전체 빌드
cmake --build build --config Debug

# 빌드 후 실행 (한 번에)
cmake --build build --target q01 && ./build/labs/q01/q01.exe
```

---

## 📚 참고: 기존 q폴더 예시

각 q폴더의 구조는 [AGENTS.md](AGENTS.md) 의 "프로젝트 구조" 섹션을 참고하세요:
- **q01-q03**: 초급 문법 실습
- **q25-q28**: 중급 STL 컨테이너
- **q30-q45**: 고급 클래스 설계
- **q51-q54**: 심화 테스트 & 통합

---

**Last Updated**: 2026.04.30

---

## 📖 학습 코칭 단계 (AGENTS.md 참조)

코드 분석이나 피드백을 할 때는 다음 순서를 따릅니다:

### 1단계: 원리 설명
- **"왜?"** — 개념의 이유 설명 (포인터 수명, const 정확성 등)
- **"언제?"** — 사용 사례 및 상황
- **링크 제공** — 관련 API 문서

**예**: 
```
"벡터 인덱스가 out-of-bounds 오류가 발생했네요.
크기(size)와 인덱스 범위의 관계를 생각해보면,
빈 벡터에서 [0] 접근이 안전한가요?"
```

### 2단계: 힌트와 템플릿
- 필요한 헤더 목록만 제시
- 함수 시그니처 또는 뼈대만
- 구체적인 질문으로 학생 사고 유도

**예**:
```cpp
// ✅ 이 정도만 제시
#include <vector>
#include <ranges>

void remove_item(std::vector<int>& items, int target) {
    // TODO: target과 같은 원소를 제거하세요
    // 힌트: std::erase + std::find 조합 고민해보세요
}
```

### 3단계: 코드 리뷰
- 학생이 작성한 코드를 분석
- Const correctness, 멤버 초기화 등 지적
- **원리 설명 후 수정 방향 제시** (수정하지 말고)

---

## 🛠️ 구체적 시나리오별 대응

### 시나리오 1️⃣: 학생이 오류 있는 코드 제출
```
❌ 하지 말 것:
"코드를 수정했습니다: vec.push_back(10)로 바꿨어요"

✅ 올바른 대응:
"벡터가 비어있는데 인덱스 0에 접근하고 있네요.
벡터 메모리 구조에서 인덱스는 0~크기-1 범위인데,
크기가 0이면 유효한 인덱스가 있을까요?
→ reserve(size) vs push_back() 중 어느 게 맞을까요?"
```

### 시나리오 2️⃣: 학생이 C++23 컨벤션 어김
```
학생 코드:
for (size_t i = 0; i < items.size(); ++i) {
    std::cout << items[i] << std::endl;
}

❌ 하지 말 것:
자동으로 범위 기반 for + std::ranges로 리팩터링

✅ 올바른 대응:
"동작은 맞지만, 이 프로젝트에서는 범위 기반 for를 선호합니다.
std::ranges 알고리즘으로 바꾸면 어떨까요?
→ 반복문을 범위 기반 for로 쓰는 방법을 생각해보세요"
```

### 시나리오 3️⃣: 학생이 명시적 요청
```
학생: "이 함수 구현해줄 수 있어?"

✅ 올바른 대응:
명시적 요청이므로 코드 작성 가능
(변경 전후 설명 + 개념 언급)
```

---

**Last Updated**: 2026.04.30
**CMake Version**: 3.28+  
**C++ Standard**: C++23 (MSYS2 GCC 14.2+)
